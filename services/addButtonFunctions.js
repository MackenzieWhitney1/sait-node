const dotenv = require('dotenv').config({ path: '.env' })
const updateButtons = document.getElementsByClassName("update-row-btn");
const deleteButtons = document.getElementsByClassName("delete-row-btn");

// to do: add functions to send manual requests